import React from 'react';
import ReactDOM from 'react-dom';

class Parameters extends React.Component {
    render() {
        return (
            <div>
                Hello world!
            </div>
        )
    }
};

ReactDOM.render(<Parameters />, document.getElementById('root'));